name: edge-workers-mutation-null
description: edge workers mutation null
argv: ["edge-workers", "enable", "headers", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

name: edge-workers-get-no-source
description: edge workers get no source
argv: ["edge-workers", "get", "headers", "--source", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

name: edge-workers-validate-name-all
description: edge workers validate name all
argv: ["edge-workers", "validate", "headers", "--all", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

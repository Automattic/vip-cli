name: edge-workers-deploy-active-update-enable
description: edge workers deploy active update enable
argv: ["edge-workers", "deploy", "headers", "--skip-build", "--app=42", "--env=develop", "--enable"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

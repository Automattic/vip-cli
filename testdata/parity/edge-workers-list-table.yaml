name: edge-workers-list-table
description: edge workers list table
argv: ["edge-workers", "list", "--format=table", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

name: edge-workers-get-missing
description: edge workers get missing
argv: ["edge-workers", "get", "missing", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

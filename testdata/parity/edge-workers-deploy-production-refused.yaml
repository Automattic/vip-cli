name: edge-workers-deploy-production-refused
description: edge workers deploy production refused
argv: ["edge-workers", "deploy", "headers", "--skip-build", "--app=42", "--env=production"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

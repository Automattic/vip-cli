name: edge-workers-artifact-symlink
description: edge workers artifact symlink
argv: ["edge-workers", "validate", "headers", "--skip-build", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

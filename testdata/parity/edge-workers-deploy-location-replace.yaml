name: edge-workers-deploy-location-replace
description: edge workers deploy location replace
argv: ["edge-workers", "deploy", "headers", "--skip-build", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

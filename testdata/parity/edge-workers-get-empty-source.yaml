name: edge-workers-get-empty-source
description: edge workers get empty source
argv: ["edge-workers", "get", "headers", "--source", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

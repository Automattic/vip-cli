name: edge-workers-validate-null
description: edge workers validate null
argv: ["edge-workers", "validate", "headers", "--skip-build", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

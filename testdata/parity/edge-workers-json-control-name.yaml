name: edge-workers-json-control-name
description: edge workers json control name
argv: ["edge-workers", "list", "--format=json", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

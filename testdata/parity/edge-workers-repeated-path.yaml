name: edge-workers-repeated-path
description: edge workers repeated path
argv: ["edge-workers", "build", "--path=.", "--path=."]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

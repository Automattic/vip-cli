name: edge-workers-deploy-inactive-update
description: edge workers deploy inactive update
argv: ["edge-workers", "deploy", "headers", "--skip-build", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

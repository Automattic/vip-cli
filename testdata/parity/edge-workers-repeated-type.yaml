name: edge-workers-repeated-type
description: edge workers repeated type
argv: ["edge-workers", "init", "--type=assemblyscript", "--type=assemblyscript"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

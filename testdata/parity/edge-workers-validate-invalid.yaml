name: edge-workers-validate-invalid
description: edge workers validate invalid
argv: ["edge-workers", "validate", "headers", "--skip-build", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

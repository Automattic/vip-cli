name: edge-workers-table-control-name
description: edge workers table control name
argv: ["edge-workers", "list", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

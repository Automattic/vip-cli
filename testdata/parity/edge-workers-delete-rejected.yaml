name: edge-workers-delete-rejected
description: edge workers delete rejected
argv: ["edge-workers", "delete", "headers", "--force", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

name: edge-workers-deploy-skip-source-create
description: edge workers deploy skip source create
argv: ["edge-workers", "deploy", "headers", "--skip-build", "--skip-source", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

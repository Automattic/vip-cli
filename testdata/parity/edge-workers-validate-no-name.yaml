name: edge-workers-validate-no-name
description: edge workers validate no name
argv: ["edge-workers", "validate", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 1

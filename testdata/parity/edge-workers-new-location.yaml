name: edge-workers-new-location
description: edge workers new location
argv: ["edge-workers", "new", "new-worker", "--location=starts_with:/api/"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

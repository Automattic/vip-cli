name: edge-workers-validate-all
description: edge workers validate all
argv: ["edge-workers", "validate", "--all", "--skip-build", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

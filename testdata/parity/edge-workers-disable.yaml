name: edge-workers-disable
description: edge workers disable
argv: ["edge-workers", "disable", "headers", "--app=42", "--env=production"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0

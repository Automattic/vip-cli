name: edge-workers-validate
description: edge workers validate
argv: ["edge-workers", "validate", "headers", "--skip-build", "--app=42", "--env=develop"]
env:
  NO_COLOR: "1"
  VIP_NON_INTERACTIVE: "1"
expect:
  exit_code: 0
